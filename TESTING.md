# 怎么验证这东西是对的

> 318 个自动化测试 + 3 个实测脚本。`npx vitest run` 一键跑完。

## 组织原则：按「守护对象」分层，不按测试手法分类

这个项目的能力由两层构成——[翻译层](README.md)（我们写的）和计算层（mathjs / nerdamer）。测试也照这条线切，每一层要证明的事情不同：

| 层 | 谁来保证 | 我们的动作 |
|---|---|---|
| **计算层** | mathjs / nerdamer 自己的测试套件 | **不重复测**，引用他们的保证并标清边界 |
| **契约层** | 我们对引擎的**配置选择** | 锁定配置产生的行为，上游改了会在这里红 |
| **翻译层** | 我们自己写的中文构式 | 测试的主体 |
| **编排层** | 意图分流、多步、追问、候选卡 | 端到端 |

这个划分不是拍脑袋的：mathjs 的 `test/unit-tests/` 与 `src/` **目录一一镜像**（`function/algebra/` 对 `function/algebra/`），另外还按执行环境分了 `browser-test-config`、`node-tests`、`typescript-tests`、`generated-code-tests`。他们按"守护什么"组织，不按"用了什么测试技巧"组织。

---

## 一、计算层：不测，引用

**为什么不测**：算数不是我们写的。测 `2+3=5` 是在测 mathjs，而他们已经测过了——`test/unit-tests/` 下 **310 个测试文件、5453 个 `it()`、14848 个断言**，跑在 Node 20/22/24 三版本矩阵 + Firefox + LambdaTest 跨浏览器上。重复一遍既没有新信息，又会在上游升级时制造假警报。

值得学的两个细节：他们的 `.mocharc.json` 设了 `forbid-only: true`（没人能靠 `it.only` 把 CI 蒙混过去）；**2296 个断言是 `assert.throws()`**，占全部断言的 15.5%，而且大量精确匹配错误消息文本而非只检查「抛了错」——这意味着错误消息是被测试锁定的契约，我们可以放心正则匹配后转成中文提示。

**我们要做的是引用他们测过什么，据此标清边界**：

| 能力 | 上游的测试依据 |
|---|---|
| 表达式解析、优先级、结合性 | `test/unit-tests/expression/parse.test.js`，含 `2^3^4 = 2.41785163922926e+24`（右结合）、`-2^2 = -4`（幂优先于一元负号）等断言 |
| 多项式求根 | `polynomialRoot.test.js`。**边界直接写在源码 docstring 里**：`polynomialRoot.js:36` 声明"operates only on linear, quadratic, and cubic polynomials" |
| Fraction 类型 | `docs/datatypes/fractions.md` 列出支持的函数，并声明**不支持的会静默转成 number**——这条直接决定了我们的数字模式调度策略 |
| 方程求解 | nerdamer `spec/solve.spec.js`，按方程类型给了实例：`solve(x^2+1,x)→[i,-i]`、`solve(8x³-26x²+3x+9,x)→[3/4,-1/2,3]`、`solve(m*x^9+n,x)→9个复根`、`solve(sqrt(x)+sqrt(2x+1)=5,x)→[4]` |
| 三角方程的**已知限制** | 同上：`solve(3sin(a²x-b)-4,x)` 只给主值 `a^(-2)*asin(4/3)`，**不给 `+2kπ` 通解**。这是我们 [BOUNDARIES.md](BOUNDARIES.md) 里那段警告的来源 |
| 积分的**已知失败** | `calculus.spec.js:161,166` 把失败也写成了断言：`integrate(cos(x^2),x)` 期望等于**它自己**。这条直接告诉我们上游是「返回未求值自身」而非抛错——我们据此加了检测 |
| 极限的**已知失败** | `calculus.spec.js` 里有 4 条被注释掉并标 `// Revisit.`，其中包括 `limit(log(x),x,0)`。极限属实验性，我们没有接入 |

**读上游测试还有一个额外收获：他们把失败也写进了测试。** nerdamer 的 `solve.spec.js` 里 `solve(x!-x^2,x)` 那条自带注释 `//NOTE: contains duplicates`，9 个「根」里 `1` 出现 4 次——这是牛顿法从不同起点收敛到同一根未去重的直接证据。这类信息文档里没有，只有读测试才知道。

完整边界清单与实测数据见 **[BOUNDARIES.md](BOUNDARIES.md)**。

---

## 二、契约层：锁定我们的配置选择

`src/engines/mathEngine.test.ts` 里有一组测的是 mathjs 的行为，但**不是在替它做质量保证**——是在锁定「我们选的这套配置产生了预期结果」：

```ts
expect(decimalOf('0.1+0.2')).toBe('3/10')       // 纯有理数分流到 Fraction 的证据
expect(decimalOf('sqrt(-4)')).toBe('2i')        // predictable:false 的证据
expect(decimalOf('cos(pi)+sin(pi)')).toBe('-1') // precision 64 + 12 位修约的证据
expect(decimalOf('2^3^2')).toBe('512')          // 幂右结合，直接影响我们的括号策略
```

这四项配置（`number: 'BigNumber'`、`precision: 64`、`predictable: false`、纯有理数分流）都是有意识的选择，每条都有理由（见 README 的设计决策）。上游改了默认行为，这里会红——这正是想要的。

同一文件里另有一组测的是**我们自己写的代码**：白名单校验（拒绝赋值、函数定义、数组、未知符号）、整数不被修约破坏、零值吸附。

---

## 三、翻译层：测试的主体

唯一完全属于我们的部分，测试密度最高。

### 中文数字（63 条）—— 搬别人的测试集

`src/core/cnNumber.test.ts` 直接移植 cn2an 的完整测试集，包括 **9 条必须抛错的负例**：

```ts
const invalid = ['百十一', '十一十二', '十七十八', '零点', '点零', ...]
it.each(invalid)('拒绝 "%s"', (input) => {
  expect(() => parseChineseNumber(input)).toThrow(ChineseNumberError)
})
```

负例比正例值钱——它们守的是「非法输入抛错不猜」。这也是选择自研而非用微软识别库的原因：那个库对这些输入会静默给出 11、22、0。

### 构式解析（68 条）—— 断言规范表达式而非数值

结构正确性最吃紧的地方，断言**翻译产物**，不碰数值：

```ts
expect(canonical('一加二乘三')).toBe('((1)+((2)*(3)))')        // 优先级
expect(canonical('三与五的和乘以二')).toBe('(((3)+(5))*(2))')   // 名词形分组
expect(canonical('二的三次方的二次方')).toBe('(((2)^(3))^(2))') // 链式幂从左往右读
expect(canonical('三加五再乘二')).toBe('(((3)+(5))*(2))')      // 顺序词
```

这四条是文法形状的关键约束。用规范串断言的好处：翻译错了，失败信息直接告诉你翻成了什么，不必二分查找是理解错还是算错。

### 词表覆盖（115 条）—— 会主动报错的测试

**每个词表条目都必须有一条用例走到它**：

```ts
it('词表里没有未被用例覆盖的条目', () => {
  const missing = declared.filter((word) => !(word in USAGE))
  expect(missing, `这些词表条目没有对应用例：${missing.join('、')}`).toEqual([])
})
```

加词表时忘了加用例，测试直接失败并点名缺哪个。写完这条当场抓出 3 个从没被测过的词（`虚数单位`、`i`、`自然对数的底`）。

### 构式相邻边界 —— 真实 bug 的出没地

**这是手工测试反复发现问题、而按功能分类的测试永远覆盖不到的一类。**

```
「二成一三」
  「三成」= 0.3   ✓ 单独测没问题
  「一三」= 13    ✓ 单独测没问题
  合起来 → TENTH 规则吞掉「二成」，剩下「一三」悬空，
           报出与真实原因（「成」是「乘」的语音误识）无关的错
```

所以成对地压边界：

```ts
// 残缺输入必须明确失败，不能吞掉半截给个数
['二成一三', null], ['一百打', null], ['三的', null],
['三与五的', null], ['以二为底', null], ['三加五再', null],

// 相邻的完整构式必须各自成立
['三成加二成', 0.5], ['根号九加根号四', 5], ['五的阶乘减四的阶乘', 96],
```

### 属性测试 —— 覆盖想不到的组合

不逐条写用例，而是枚举组合并断言性质：

```ts
// 7 个数 × 4 个运算 × 7 个数 = 196 种组合
// 允许失败（明确报错），但不允许算成别的数
if (got !== null && Math.abs(got - expected) > 1e-9) wrong.push(...)

// 三操作数：2×4×2×4×2 = 128 种组合，按优先级算出期望值逐个比对
```

---

## 四、编排层与不变量护栏

`src/core/calculator.test.ts`（49 条）覆盖端到端：多步问句、跨轮追问、方程、候选卡、LaTeX 回显。

其中三条专门守核心设计不被后续改动破坏：

```ts
it('多处歧义时明确拒绝，不静默选定其中一种读法', ...)
it('数学错误标记为 math，不会被转交 AI 重新翻译', ...)
it('全括号输出可被引擎无歧义地重解', ...)
```

最后一条：文法产出的串重新解析后结果必须一致。这比肉眼检查括号可靠得多。

---

## 五、bug 回归：每个手工发现的问题留一条

```ts
it('「成」后跟数字不再吞掉左操作数', () => {
  // 曾经：「成」按「三成=0.3」处理，吞掉「二成」，剩下「一三」悬空
  expect(tryCompute('二成一三')).toBe(null)
  expect(tryCompute('三成')).toBeCloseTo(0.3)   // 正常用法不受影响
})

it('整数结果不被有效数字修约破坏', () => {
  // 曾经 factorial(20) 被 12 位修约成 2432902008180000000
  expect(tryCompute('二十的阶乘')).toBe(2432902008176640000)
})
```

每条都注明当初错在哪。这类用例证明的是「同样的坑不会踩第二次」。

---

## 最重要的那条不变量

计算器有个特殊性质：**算错比报错糟糕得多**。用户看到「读不懂」会换个说法，看到一个数字则直接采信。所以整套测试真正围绕的是：

> **任何输入，要么算对，要么明确失败——绝不给出一个错误答案。**

这也是为什么词表覆盖、边界、属性三类测试都以「不能算成别的数」为判据，而不是以「功能覆盖率」为判据。

---

## 模型评测：有随机性，不进 CI

Whisper 和 LLM 要跑模型、有随机性，做成独立脚本：

```bash
./scripts/make-audio.sh                        # 系统 TTS 合成中文测试音频
node scripts/eval-voice.mjs base small turbo   # 三档 Whisper 对比
node scripts/eval-llm.mjs                      # LLM 翻译质量（默认 DeepSeek，见下节）
node scripts/probe-boundaries.mjs              # 能力边界实测（BOUNDARIES.md 的依据）
```

**语音评测的关键设计：判据是端到端算得对不对，不是文本一字不差。**

Whisper 把「一加一」写成「1加1」完全无害（构式层支持中阿混写），按字符比对会误判成失败：

| 档位 | 文本完全一致 | **端到端算对** |
|---|---|---|
| base | 0/5 | 1/5 |
| small | 0/5 | 0/5 |
| turbo | 0/5 | **4/5** |

三个档位的「文本完全一致」都是 0，但端到端是 1:0:4——只看前一个指标会得出「三个模型一样烂」的错误结论。**判据选错，数据再多也没用。**

---

## LLM 翻译实测

**2026-07-30，`deepseek-v4-flash`，14/14**（单条 1.2–3.4s）。

这一轮是**穿过部署链路**打的——`node scripts/serve.mjs` 起在本机，评测脚本指向
`http://localhost:4999/api/llm/v1` 且不带任何凭据，与面试官从 ngrok 打开页面时走的是同一条路。
所以它同时证明了两件事：模型翻译得对，以及转发代理的护栏（模型白名单、`max_tokens` 封顶等）
没有把正常请求也拦掉。复跑：

```bash
node scripts/eval-llm.mjs                                          # 默认 DeepSeek + deepseek-v4-flash
node scripts/eval-llm.mjs --model deepseek-v4-pro
node scripts/eval-llm.mjs --base-url http://localhost:11434/v1 --model qwen2.5:1.5b
node scripts/eval-llm.mjs --base-url http://localhost:4999/api/llm/v1 --model deepseek-v4-flash
                                                                   # ↑ 穿过 serve.mjs 的转发代理
```

key 只从环境读（`DEEPSEEK_API_KEY`，或项目根目录的 `.deepseek.key`），脚本里不写死也不打印；
本地 Ollama 不需要 key，脚本也**不会**把 key 递给本地地址。

14 条用例覆盖 LLM 在这个架构里要担的全部职责——它只提议读法，不算数：

| 类别 | 用例 | 判据 |
|---|---|---|
| 基础对照 | 「一加一等于几」「二加二乘二等于几呀」 | 2 / 6。规则本来就能读懂，用来验模型不会读岔 |
| 大数 | 「e的100次幂是多少」 | 结果含 `2.688…e+43` |
| 语音同音字 | 「根号酒是多少」 | `sqrt(9)` = 3。turbo 实测会把「九」听成「酒」 |
| 口语绕弯 | 「三个苹果加五个梨一共几个」「把十二平分成四份每份多少」 | 8 / 3 |
| 生活场景 | 「一百块打七折再减二十」 | 50 |
| 嵌套运算 | 「五的立方再开平方」 | 11.18——考的是括号规范性，`(5)^3^(1/2)` 会被右结合读成 16.24 |
| **真歧义** | 「六除二」 | **至少两个候选**，且同时含 3 与 1/3。这条测的是「模型没有替用户拍板」 |
| 符号计算 | 「解方程x平方加一等于零」「x平方求导」「二x的积分」 | 含 `i` / `2x` / `x²` |
| **拒答** | 「今天天气怎么样」「帮我写一首诗」 | **不许硬凑算式**：调 reject 工具最好，给不出候选也算过 |

**判据全部可判定**（结果精确相等，或正则匹配「算式 → 结果」整行），所以「过了几条」是算出来的，
不是看出来的——这正是上一节说的「判据选错，数据再多也没用」。

三个设计上的取舍：

**每条都送去 `translate()`，哪怕规则本来就能算。** 脚本会顺带打印规则的处理结果作参照，但不因为
「规则搞定了」就跳过模型——那样分母会随规则覆盖面浮动，14/14 就不是个能比较的数。

**拒答的判据是「没有给出错答案」，不是「说了漂亮的拒绝理由」。** 显式调 `reject` 最好，
但一个候选都给不出同样过——用户看到的都是「读不懂」，而不是一个凑出来的算式。

**真歧义要求两个候选。** 只给一个就算错，哪怕那一个是对的——替用户拍板本身就是这个项目要避免的行为。

模型大小是有差别的。同一套用例、同一天，本机 `qwen2.5:1.5b`（Ollama）只过 **6/14**：符号计算、
真歧义、括号规范性都过不了。这条数据的用处不是「小模型不行」，而是**这套用例有区分度**——
如果换个模型分数不动，那说明用例太松，测不出东西。

---

## 跑测试

```bash
npx vitest run
```

```
src/core/cnNumber.test.ts           63 个   翻译层：中文数字（含 9 条负例）
src/core/nlu.test.ts                68 个   翻译层：构式解析
src/core/lexicon-coverage.test.ts  115 个   翻译层：词表覆盖 + 边界 + 属性 + 回归
src/core/calculator.test.ts         49 个   编排层：端到端 + 上游静默失败拦截
src/engines/mathEngine.test.ts      23 个   契约层：引擎配置 + 白名单
                                   ─────
                                    318 个
```

## 已知的测试盲区

诚实地说，这些还没覆盖：

- **UI 交互**没有自动化测试（候选卡点选、语音按钮状态、设置面板），靠浏览器手工验证
- **并发**：连续快速提交多条输入时的状态竞争
- **超长输入**的性能边界
- **语音评测样本只有 5 条**，且全部由同一个 TTS 音色合成，不能代表真实口音与噪声环境
- **LLM 评测每次结果可能不同**（虽然设了 `temperature: 0`），14 条用例样本量仍然偏小；且它打的是真实服务，网络与服务端版本都会影响结果
- **转发代理没有自动化测试**，护栏是 curl 手工验的（清单见 [BOUNDARIES.md](BOUNDARIES.md)）
- 没有跨浏览器测试（mathjs 有 `lambdatest.yaml` 做这件事，我们没有）
