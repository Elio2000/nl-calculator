# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 语音模型权重（约 1.3G）。不进 git：仓库会爆，而且没有它也能跑——
# 本地没有权重时 transformers.js 会自动回落到 HuggingFace 下载（见 README）
public/models/

# 凭据。key 只来自运行环境，绝不进仓库
.deepseek.key
*.key
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 工具与环境局部配置
.claude/
