# 能力边界

> 所有结论都来自实测与源码核验。复现：`node scripts/probe-boundaries.mjs`
>
> **审计基线**（边界会随版本变化，换版本请重新核验）：
> mathjs `15.2.0` · nerdamer-prime `1.5.0`（上游 nerdamer 审计基线 `1.1.13`）

## 首要限制：绝不生成左置 `%`

mathjs 的 `%` 是**取模与百分比双关**，判定规则藏在 `parseUnaryPercentage` 里：

```js
100+3%   → 103     ✓ 百分比
3%+100   → 3       ✗ 被当成 3 mod 100，且不报错
50%(13)  → 11      ✗ 被当成 50 mod 13，不是 13 的一半
80% pi   → 1.46    ✗ 80 mod π
```

依据：`test/unit-tests/expression/parse.test.js:1618-1673`，其中 `3%+100` 那条断言旁边就写着注释 `// treat as 3 mod 100`。

**我们的应对**：翻译层把「百分之二十」一律展开成 `((20)/100)`，永不生成 `%`。这条在
[TESTING.md](TESTING.md) 里有测试守着。同理，隐式乘法也一律不用——mathjs 作者自己在
`docs/expressions/syntax.md` 里写「这套规则可能在未来大版本调整，不确定就用显式 `*` 和括号」。

## 先说清楚三类边界的区别

这个项目的能力由两层决定：**翻译层**（我们写的，中文→表达式）和**计算层**（mathjs / nerdamer）。所以"做不到"有三种完全不同的原因，混在一张清单里会误导人：

| 类别 | 含义 | 能不能补 |
|---|---|---|
| **A 计算层做不到** | 引擎本身没这个能力 | 换引擎才行，是硬边界 |
| **B 计算层能做，翻译层没接** | 引擎有这个函数，但我们没写对应的中文构式 | 加词表 + 加文法分支，是工作量问题 |
| **C 翻译层的主动取舍** | 能做但故意不做 | 有明确理由，见下 |

B 类是最容易被误读的——`1米加2厘米` 报「看不懂"米"」，看起来像算不了，其实 **mathjs 自带完整的单位系统**，只是我们的分词器不认识「米」这个字。

---

## 能做什么（实测）

### 基础运算与精度

| 输入 | 结果 | 说明 |
|---|---|---|
| `0.1+0.2` | `3/10` | 精确，不是 0.30000000000000004 |
| `0.1+0.2-0.3` | `0` | 精度不累积 |
| `1/3+1/6` | `1/2` | 分数精确运算 |
| `1除以3乘以3` | `1` | 除后乘回不掉精度 |

靠 mathjs 的 Fraction 类型（纯有理数表达式）和 BigNumber（precision 64）。

### 大数

| 输入 | 结果 |
|---|---|
| `20的阶乘` | `2432902008176640000`（19 位，精确） |
| `100的阶乘` | `9.33262154439e+157` |
| `2的100000次方` | `9.99002093014e+30102`（3 万位） |

30 位以内的整数原样给出，超过转科学计数法。

### 幂、根、三角、对数

| 输入 | 结果 | 备注 |
|---|---|---|
| `负8的三次方根` | `-2` | 奇次根取实根 |
| `根号负4` | `2i` | 偶次根提升复数 |
| `sin30度` | `0.5` | 带「度」自动按角度制 |
| `cos派` | `-1` | 不带「度」按弧度制 |
| `sin派` | `0` | 数值噪声（3.08e-64）被吸附 |
| `以2为底1024的对数` | `10` | 任意底 |

### 复数

| 输入 | 结果 |
|---|---|
| `i乘以i` | `-1` |
| `根号负4加2` | `2 + 2i` |
| `(1+i)的2次方` | `2i`（实部噪声已吸附） |
| `ln负1` | `3.14159265359i` |

### 方程（按次数实测）

| 次数 | 例子 | 结果 |
|---|---|---|
| 一次 | `2x+3=7` | `x = 2` |
| 二次实根 | `x²-4=0` | `x = 2, -2` |
| 二次复根 | `x²+1=0` | `x = i, -i` |
| 三次 | `x³-1=0` | 三个根（含复根） |
| 四次 | `x⁴-16=0` | `2, -2, 2i, -2i` |
| 五次及以上 | `x⁵-32=0` | 数值全根 |

nerdamer 对 1–4 次用闭式公式，更高次用 Jenkins–Traub 数值求根。

### 超越方程

| 输入 | 结果 | 注意 |
|---|---|---|
| `2的x次方=8` | `x = 3` | 指数方程 |
| `lnx=1` | `x = 2.7182818285` | 对数方程 |
| `cosx=x` | `x = 0.7390851332` | 无闭式解，牛顿迭代 |
| `sinx=0` | `0, ±π, -2π…` | ⚠️ 见下方警告 |

> ⚠️ **三角方程的解可能不完整。** nerdamer 用数值求根，解集取决于它的采样参数。我们按到零的距离排序、只显示最近的几个并注明「还有其他解」——但**解集本身就可能不全，不只是被显示截断**。

### 解集不完整的几类方程（Codex 实证）

nerdamer 对某些方程只给出**一个**解，而实际有无穷多：

| 方程 | 它给的 | 实际 |
|---|---|---|
| `sqrt(x²)=x` | `x = 0` | 所有 x ≥ 0 |
| `abs(x)=x` | `x = 0` | 所有 x ≥ 0 |
| `sin(x)=0` | 最接近零的几个 | `x = nπ`，无穷多 |

**恒等式我们会明确拦下来**（`x=x`、`x/x=1`、`log(exp(x))=x` 都会说明「恒成立」，
而不是像上游那样返回一个具体的值——对 `x/x=1` 来说 `x=0` 恰恰是唯一**不**成立的点）。
但上面这类「部分区间恒成立」的方程识别不出来，会给出一个不完整的解集。

### 数值求根找不到哪些根（有源码依据）

nerdamer 的求解参数写在 `Solve.js:40-77`，每一条都推出一个具体的盲区：

| 参数 | 值 | 后果 |
|---|---|---|
| `SOLVE_RADIUS` | 1000 | **\|x\| > 1000 的根找不到** |
| `ROOTS_PER_SIDE` | 10 | 每侧最多 10 个根，周期函数必被截断 |
| 扫描步长 | 0.1 / 0.05 / 0.01 | 间距 < 0.01 的相邻根会被跳过 |
| 基于**符号变化**找根 | — | **偶重根（切于 x 轴，如 `(x-2)²`）不产生符号变化，数值路径找不到** |
| 种子点全为实数 | — | **数值路径找不到复根**（复根只在 ≤4 次的符号路径出现） |

精度：≥5 次多项式走 Jenkins–Traub，**只保证 7 位小数**（`Algebra.js:1019`：`decp = decp || 7`）。

### 返回的分数可能是近似值，不是精确值

nerdamer 有时返回像 `832040/2178309` 这样的分数，看着像精确解，实际是浮点根的有理逼近——
这个数偏离真值 `(3−√5)/2` 达 **9.4e-14**。机制在 `Solve.js:493-495` 的注释里写明：
「转成小数再转回分数，会损失一些精度，但好过溢出」。

**我们的应对**：识别出这类巨大分数（分子分母都 ≥5 位）时，直接显示小数而非分数形。

### 对数的语义陷阱

**两个引擎里 `log` 都是自然对数，但中文习惯里 log 指常用对数**——差一个数量级。

我们的处理：中文说「log」翻译成 `log10`，说「ln」才翻译成 `log`；显示时反过来改写
（`log10(100)` 显示成 `lg(100)`、`log(100)` 显示成 `ln(100)`、`log(8,2)` 显示成 `log₂(8)`），
两头都对得上中文读者的直觉。LLM 的工具契约里也写明了这一点，否则模型会按中文习惯
生成 `log(100)` 却期望得到 2。

### 微积分

| 输入 | 结果 |
|---|---|
| `x平方求导` | `2·x` |
| `sinx求导` | `cos(x)` |
| `x平方求积分` | `(1/3)·x³ + C` |
| `sinx求积分` | `-cos(x) + C` |
| `e的x次方求积分` | `e^x + C` |
| `lnx求积分` | `-x + log(x)·x + C`（分部积分） |

**算不出来的积分会明确报错**，不会伪装成算出来了。nerdamer 遇到没有初等原函数的积分
（如 `∫cos(x²)dx`）时**不抛错**，而是返回未求值的 `integrate(cos(x^2),x)`。
我们检测到结果里还留着 `integrate(` / `limit(` / `diff(` 就判定为失败——
不检测的话会给它加个「+ C」端出去，把「没算出来」伪装成「算出来了」。

已知算不出的积分（nerdamer 自己的测试里就标着）：`∫cos(x²)dx`、`∫1/tan(ax)ⁿdx`、
以及 `sin(x)ⁿ/cos(x)ᵐ` 当 m>n（`Calculus.js:1611` 注释：`we don't know how to handle … yet`）。

**两个引擎的函数集不一样。** mathjs 认识 `gamma`，nerdamer 不认识——它会把函数名当成变量，
`diff(gamma(x))` 退化成一个光秃秃的 `gamma`。我们检测这种退化（原式含变量、结果却只剩
一个裸标识符）并明确报错，不让它冒充答案。

---

## 做不到什么

### A 类：计算层的硬边界

| 能力 | 状况 |
|---|---|
| **符号微分方程** | JS 生态确实没有。nerdamer 无 `dsolve`，Algebrite 的 `dsolve` 是空壳（`eval.ts:297` 直接 `stop('dsolve')`）。libqalculate 只支持一阶可分离/线性，且是 GPL + C++。要做只能上 Pyodide + SymPy（架构已预留，未实现） |
| **区间算术 / 不确定度传播** | 只有 libqalculate 有（MPFR 上下界） |
| **三角方程通解** | 见上方警告 |

### B 类：计算层能做，我们没接

**这些不是算不了，是中文构式没写。** 列出来是为了说明边界在哪、代价是多少：

| 能力 | 引擎侧现状 | 缺的是什么 |
|---|---|---|
| 单位换算 | mathjs 有完整单位系统（`5 km + 3 m`、`in`/`to` 转换） | 中文单位词表 + 「换算成」构式 |
| 矩阵 | mathjs 有 `det`/`inv`/`eigs`/`transpose` | 中文矩阵字面量的写法设计 |
| 极限 | nerdamer 有 `limit(expr, x, 0)` | 「x 趋近于零时…的极限」是个三段式构式，不小 |
| 高阶导数 | nerdamer 的 `diff` 支持阶数参数 | 「二阶导数」的构式 |
| 方程组 | nerdamer 有 `solveEquations()` | 「…且…」的连接构式 + 多方程意图识别 |
| 数值 ODE | mathjs 有 `solveODE`（RK23/RK45） | 初值问题的中文表述设计 |

### C 类：主动的设计取舍

| 取舍 | 理由 |
|---|---|
| 一句话里多个裸「除」直接拒绝 | 每个「除」有两种读法，两个就是四种候选，摆一堆卡片比让用户改说「除以」更糟 |
| 名词形分组只支持简单操作数 | `三与五的和` 可以，`一与二的和与三的和` 不行。嵌套名词形的歧义快速增长，干净失败比猜好 |
| 多未知数方程会解但会声明 | `解方程x加y=3` → `x = -y+3（式子里还有 y，所以只能用它表示 x）`。数学上正确，但要讲明白不是具体数值 |
| 百分比不走引擎的 `%` | mathjs 里 `50%(13)` 是取模得 11，三家引擎语义还各不相同。一律在翻译层展开成显式除法 |

---

## 翻译层自身的边界

计算层之外，中文理解本身也有边界：

| 情况 | 行为 |
|---|---|
| 词表里没有的说法 | 报「看不懂 X」，可转 AI 翻译 |
| 语音同音字错误 | 规则读不懂 → AI 纠音（`根号酒` → `sqrt(9)`），实测有效 |
| 构式残缺（`三的`、`一百打`） | 明确失败，不吞掉半截给个数 |
| 完全非数学的话 | 明确失败 |

**共同的底线：宁可报错，绝不给出一个错误答案。** 这条有专门的属性测试守着（见 [TESTING.md](TESTING.md)）。

---

## 部署形态的边界

这两条不是「算不算得出来」的边界，是**把它端出去给别人用**时的边界。

### 转发代理是一条白名单，不是一个通道

默认链路里浏览器不直接碰 DeepSeek，而是请求同源的 `/api/llm`，由服务端补上凭据再转发
（开发时 `vite.config.ts`，部署时 `scripts/serve.mjs`）。好处是页面里没有 key；代价是这个洞一旦
用 ngrok 暴露到公网，任何人都能打。所以它只放行必要的那一点点：

| 护栏 | 值 | 挡的是什么 |
|---|---|---|
| 路径白名单 | 只认 `POST /api/llm/v1/chat/completions` | 其余 `/api/**` 一律 404，不让它长成通用 OpenAI 代理 |
| 模型白名单 | `model` 必须以 `deepseek` 开头 | 别人拿它去点名调别的贵模型 |
| 请求体上限 | 64KB | 翻译一句中文用不到 1KB |
| 输出上限 | `max_tokens` 强制 ≤ 4096（没给也补上） | 拿它当写作机 |
| 请求头 | 服务端**新建**请求头，不继承客户端的 | 浏览器里残留的旧 BYOK key 盖掉服务端凭据 |
| 缺 key | 直接回 500 JSON，不转发 | 把没有认证的请求送出去挨 401，看起来像「服务坏了」 |

自测（假 key 起在 4999，DeepSeek 回 401 属预期）：

```
GET  /                              → 200 text/html
GET  /whatever                      → 200 text/html   SPA 回退
GET  /models/nope/config.json       → 404             带扩展名的必须 404，见下
POST /api/llm/v1/chat/completions  {"model":"gpt-4o"}    → 400 只允许 deepseek 系列模型
POST /api/llm/v1/chat/completions  80KB body            → 413 请求体超过 64KB 上限
POST /api/llm/v1/models                                 → 404 没有这个接口
GET  /api/llm/v1/chat/completions                       → 404 没有这个接口
POST /api/llm/v1/chat/completions  {"model":"deepseek-v4-flash",…} → 401（假 key，说明真的转发出去了）
缺 key 时同一请求                                        → 500 服务端未配置 DEEPSEEK_API_KEY
```

倒数第三条那个 404 不是随手写的：语音模型走 `/models/...` 取权重，本地没有权重时
transformers.js 靠 **404** 判断「去 HuggingFace 下」。如果 SPA 回退对任何路径都回 index.html，
它会拿到一段 HTML 当 JSON 解析而硬失败。所以回退只给**没有扩展名**的路径。

### ngrok 预览时语音模型建议切 base 档

默认的 turbo 档是 1GB 权重，本机加载几秒，走 ngrok 隧道首次加载要几分钟甚至超时——
远程访问时在设置里切到 **base（78MB）**。代价是识别质量下降（实测端到端 1/5 对 4/5，见
[TESTING.md](TESTING.md)），但语音只是输入方式之一，文本输入不受影响。
第二次访问会命中浏览器缓存，只有首次这么慢。

---

## 怎么复现这份文档

```bash
node scripts/probe-boundaries.mjs
```

输出直接对应上面的表格。改了代码之后重跑，边界变了就更新这份文档。
